<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="default.list">
      <field name="name">default list</field>
      <field name="model">default.default</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="default.action_window">
      <field name="name">default window</field>
      <field name="res_model">default.default</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="default.action_server">
      <field name="name">default server</field>
      <field name="model_id" ref="model_default_default"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="default" id="default.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="default.menu_1" parent="default.menu_root"/>
    <menuitem name="Menu 2" id="default.menu_2" parent="default.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="default.menu_1_list" parent="default.menu_1"
              action="default.action_window"/>
    <menuitem name="Server to list" id="default" parent="default.menu_2"
              action="default.action_server"/>
    -->
  </data>
</odoo>